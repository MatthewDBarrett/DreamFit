<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ethan.dream_fit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainPage" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:screenOrientation="portrait"
            android:name=".StepCounter"
            android:parentActivityName=".MainPage">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainPage" />
        </activity>
        <activity
            android:name=".bmi_calculator"
            android:parentActivityName=".MainPage" />
        <activity
            android:name=".calorie_tracker_add_item"
            android:parentActivityName=".MainPage" />
        <activity
            android:name=".calorie_tracker_add_list"
            android:parentActivityName=".calorie_tracker_add_item" />
        <activity
            android:name=".profile_page"
            android:parentActivityName=".MainPage"></activity>
        <activity android:name=".calorie_tracker_main_page"></activity>
    </application>

</manifest>